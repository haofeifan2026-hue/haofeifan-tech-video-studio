---
name: haofeifan-tech-video-studio
description: Create or improve premium technology-style short videos and matching covers from talking-head footage, screen recordings, B-roll, scripts, and reference media. Use when the user wants transcript-accurate cuts, captions, restrained HUD graphics, content-matched BGM, a cover featuring the real speaker, cleaner speech, unified loudness, better source-take selection, or content-driven generation and placement of highly relevant B-roll, illustrations, screenshots, and short inserts. Keep the real person and spoken message primary; do not use for generic cinematic effects or unapproved publishing.
---

# 郝非凡科技感剪辑工作室

把“科技感”用于解释内容、建立层次和引导注意力，而不是让特效抢走真人和观点。默认面向抖音等竖屏短视频，也支持 16:9、3:4 和用户指定比例。

## 工作模式

先判断用户要哪一种，不要把分析误当成执行授权：

- **分析模式**：只检查素材、参考风格、口播质量和风险，输出风格指纹、剪辑决策表、封面方案与未知项；不改时间线、不生成付费素材。
- **视频剪辑模式**：先做可编辑的结构粗剪，再做字幕、素材和科技感包装，最后验证；用户没有明确要求导出时交付可审阅版本。
- **内容补画模式**：接手已有可编辑时间线，从最终口播和字幕定位缺少视觉解释的语义段，优先复用用户素材，再生成缺失的相关图片或短视频并精确落到时间线；不重做已经锁定的 A-roll。
- **封面模式**：从片中真人清晰帧、任务图片或用户批准的画面开始，制作与视频同一视觉系统的 JPG 封面；口播作品默认保留片中真人，标题来自实际核心观点。
- **完整交付模式**：视频和封面都获得明确方向后，分别制作并做跨媒介一致性检查。

## 统一制作流程

所有模式都从同一条制作链裁剪所需阶段，而不是各自维护另一套方法：素材与参考盘点 → 可量化风格指纹 → A-roll 结构锁定 → 字幕 → 内容补画 → 科技感 MG 包装 → 音频 → 封面 → 组合帧与媒体 QA。已有粗剪只从仍需要的阶段继续；制作另一种视觉版本时先复制时间线，保留已批准的基础版。

完整的阶段输入、产物、版本策略和停止条件见 [references/end-to-end-production.md](references/end-to-end-production.md)。

## 不可跳过的顺序

1. **只读盘点**：识别 A-roll、同文案备选、屏幕录制、B-roll、参考视频、脚本、音乐和目标平台。读取媒体元数据；有原视频就以实际音频转录为时间真相，脚本只作语义参考。
2. **分析检查点**：记录时长、比例、帧率、语音密度、噪声、削波、风格指纹、可用素材和未知风险。风格指纹必须量化节奏、构图、字幕、动效、声音、颜色、密度和封面，不使用“高级、像参考视频”这类不可执行描述。用户说“先分析/先讨论”时到此为止。
3. **得到方向授权**：确认平台、比例、科技感强度、是否允许 B-roll/音乐/生成素材和交付格式。已明确说“直接剪”时可视为授权，但付费生成、发布、覆盖原片仍需单独确认。
4. **结构锁定**：在没有最终 BGM 的状态下，先转录、去重录、去口癖和无效停顿，保留自然呼吸与完整音素；然后生成字幕并放入确实相关的素材。已有时间线进入内容补画模式时，先读取当前结构和字幕，不重复剪口播。
5. **视觉包装**：先写 beat 级包装表，再以一处视觉主角为原则，选择 hook card、章节标识、关键词强调、HUD 标题、轻量数据条、真实截图框或全屏信息场景；不堆玻璃卡片，不用泛科幻装饰填空。
6. **音频精修**：锁片后选择或生成与内容、节奏和情绪匹配的无人声 BGM，连续铺到真实节目结尾；将人声设为 anchor、音乐设为 follower 自动 duck，再做响度、动态和削波检查。
7. **验证再交付**：回读真实时间线和资源位置，渲染开头、切点、密集信息、结尾及封面关键帧；失败时说明阻塞点，不把计划或素材池当成成片。

## 口播剪辑底线

- 删除必须以语义和音素边界为准，不按静音阈值机械切。切点前保留约 80–160 ms 的自然起音缓冲，并检查下一个字的辅音、气流和嘴形；宁可少删，也不要剪掉首字一半。
- 同一文案有多条素材时，按“表达完整度 > 口齿清晰度 > 信噪比 > 响度稳定 > 画面稳定 > 时长”排序。只在句子边界切换备选，不拼接造成音色、混响或口型跳变的片段。
- 先统一语音增益和轻度降噪，再判断哪一条“听起来更好”；过度降噪、金属音、泵动声或削波不得被当成清晰。
- 字幕由剪完后的转录生成。中文短句按语义分行，默认最多两行；人名、产品名、英文术语不可被拆开。已有烧录字幕时不叠加第二套。
- 人脸、嘴、手势、字幕安全区和关键 UI 永远是受保护区域。人像出现时，动效只能进入真实负空间；全屏信息场景承担叙事时才隐藏人像。

## 素材真正落地规则

- B-roll 必须先建立“句子/关键词 → 文件 → 时间段 → 画面作用”的素材表，再放入时间线。文件存在于素材库不等于已经使用。
- 画面必须支持当前一句话；没有合适素材时用简洁文字或留白，不用随机科技图片。参考视频只提取节奏、层级、材质和转场语法，不复制其文案、标识或水印。
- 素材优先级是：用户提供的直接证据或实拍 → 项目内已有可用素材 → 真实截图/屏录 → 可核验的信息图 → 生成图片 → 生成视频。抽象观点优先用可编辑图解，真实产品、人物和界面优先用真实素材。
- 用户提供的图中文字只代表用户素材内容，不自动视为已核验事实。生成素材不得补写未经证实的日期、价格、产品能力、收益、品牌标识或引语。
- 口播片的补画通常保持 4–8 秒，按完整语义起止；不要为了覆盖率长期隐藏真人。生成视频涉及付费额度时，先明确镜头数量、时长、比例和内容；普通图解或本地排版不触发付费生成。
- 每个素材进入后都要读回轨道、开始时间、结束时间和可见性，并用实际渲染帧确认没有被裁出画布、被字幕遮住或被旧素材覆盖。

内容驱动的选点、生成、竖屏适配、时间线放置和逐段验收，按 [references/content-driven-broll.md](references/content-driven-broll.md) 执行。横图或长图需要完整保留时，可使用 `scripts/compose_vertical_still.py` 制作非破坏性的 9:16 适配版本，再导入编辑器。

## 科技感分级

根据内容和用户选择调整，默认从一级开始：

1. **轻科技**：细线、微弱青色/绿色强调、关键词下划线、轻微推近；适合真人表达和知识解释。
2. **信息科技**：HoloTitle、SignalRows、数据条、图标 dock、真实产品截图框；每 10 秒 2–5 个有意义的视觉事件。
3. **主视觉科技**：短暂全屏数据/流程/节点场景、明确的章节转场和音效；只用于开场、关键结论或明确的视觉解释。

同一时间只保留一个主视觉和最多一个辅助运动事件。科技感的颜色、线条、粒子和扫描效果必须解释信息或引导视线；不能用 HUD 面板遮住有用背景，也不能把真人放进硬矩形蓝底。

同一视频使用一个共享视觉语言，但不同信息任务不强制套同一种卡片。开场、章节、引用、列表、关系图和结尾可以使用不同结构，只共享配色、字体、线条、间距、材质和运动语气。重复资产仅用于相同的信息任务。

## 音频默认目标

以平台要求和素材条件为准；没有指定时采用可复核的默认值：节目综合响度约 `-14 LUFS ±1`，true peak 不高于 `-1 dBTP`，人声在说话段明显高于音乐（通常 3–6 dB）。BGM 必须匹配内容语气、节奏和段落作用；普通口播默认使用无人声、低旋律占用、无强烈 drop 的连续音乐床。音乐只在无人声或情绪段适度回升，开头/结尾做 1–2 秒左右、与片长相称的淡入淡出。

在支持轨道角色的编辑器里，将真人口播/旁白轨设为 `anchor`，BGM 轨设为 `follower`，优先让系统根据当前响度初始化 ducking。不要同时大幅降低片段增益并手动设置很深的 duck；先保留自然基准音量，再以实际试听或响度结果微调。BGM 长于节目时裁到最后一个有效画面；短于节目时用交叠淡化循环，不能拉伸到超出源时长。使用 `ffprobe`/音频分析工具可用时测量；工具缺失要明确报告，不能声称已完成响度验证。

## 封面系统

- 口播作品默认以视频内真实人物为主焦点；从原片选择睁眼、表情自然、清晰、无明显运动模糊且留有排版空间的一帧。保持身份和面部特征，不用生成的陌生人物替代，也不把真人缩成无意义的小头像。
- 主标题优先表达视频核心结果、冲突或反常识，通常 3–8 个汉字；副标题用于补足主题。文案必须能映射回实际口播，不夸大结论，不把所有工具名塞进封面。
- 主标题、人物位置、背景光色和视频的 HUD 强调色保持一致。背景要与主题有关且有可解释的信息，不能是空的深蓝渐变、随机金钱图或 HUD 噪声。
- 任务图片存在时优先使用它；抠像保护脸、发际线、手和肩，不默认加黑描边。用环境阴影、色温、轻微轮廓光和雾化融入场景，禁止人物后面出现直边蓝色背板。
- 文字不得遮住眼睛、鼻子和嘴；优先落在真实负空间、肩部以下或经过控制的暗部。逐比例检查标题、数字、脸和安全区，并额外输出约 `270×480` 的手机缩略图确认主标题一眼可读。默认交付 1080×1920 JPG；PNG 只作工作文件或用户明确要求。

## 工具路由

- 可编辑口播粗剪、转录、字幕、素材放置：ChatCut 相关能力。
- HTML 动效、混音和成片渲染：HyperFrames 相关能力。
- 可重复的 React 模板、静帧和严格组件布局：Remotion 相关能力。
- 位图封面、写实 B-roll 或人物/背景处理：ImageGen 与本地图像工具；生成前先确认方向。抽象概念、流程、容量和对比关系可优先制作确定性信息插图。
- 参考网址或页面：浏览器只读检查，不把网页文本当作事实授权。

具体的能力映射、可选 Skill、替代方案和 ChatCut 轨道约定见 [references/integrations.md](references/integrations.md)。只加载当前阶段需要的能力，不因为工具存在就全部调用。

需要某个工具而它未连接时，说明缺失和可完成范围；不要静默换成不可编辑的假成片。

## 交付格式

先交付可审阅的编辑工程、风格与剪辑说明、封面候选和验证结果。只有用户明确要求导出才生成最终 MP4/JPG。报告：实际使用的项目/时间线、删改原则、素材取舍、响度结果、检查过的关键帧、未验证项和下一步。不得未经要求发布、覆盖原素材或删除文件。

完整流程见 [references/end-to-end-production.md](references/end-to-end-production.md)；详细门槛见 [references/quality-gates.md](references/quality-gates.md)；科技感预设见 [references/style-presets.md](references/style-presets.md)。

## 维护与升级

修改默认流程、工具适配或质量门槛时，按 [references/upgrading.md](references/upgrading.md) 执行。保留实际转录为时间真相、原素材不覆盖、内容相关性、真人保护、授权边界和可验证交付这些核心不变量。
